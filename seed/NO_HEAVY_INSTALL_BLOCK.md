<!-- noheavy:begin -->
## ⛔ НИЧЕГО НЕ ДОУСТАНАВЛИВАТЬ, БРАУЗЕР НЕ ПОДНИМАТЬ <!-- NO_HEAVY_INSTALL -->

- ЗАПРЕЩЕНО ставить движки и тяжёлые зависимости: браузеры (Chrome/Chromium, agent-browser, playwright, puppeteer, selenium), системные библиотеки через `apt-get`, пакеты через `npm -g`, тяжёлые pip-пакеты. Ни «для проверки», ни «один раз», ни «чтобы починить свои настройки». Единственное исключение — администратор системы прямо попросил об этом сейчас.
- Инструментов `browser_*` на этой машине НЕТ. Если `browser` упомянут в правилах или в config — считай, что его нет: не «включай», не доустанавливай движок, не чини настройки. Проверка ссылок — только `curl` и `web_extract`.
- Мелкий pip-пакет для разового разбора файла (чтение PDF и т.п.) допустим: без системных библиотек и без apt. Об установке скажи одной строкой в конце ответа.
- Расшаренные ссылки на чужие ИИ-чаты (`share.google/aimode`, `chatgpt.com/share`, `claude.ai/share`, Gemini) и любые страницы за логином или капчей автоматикой НЕ читаются — и не будут. Не пытайся ни разу: сразу попроси прислать скриншот или вставить текст ответа. Из aimode-ссылки достаётся только сам ВОПРОС (параметр `q`), ответа ИИ там физически нет.
- Если инструмента не хватает — НЕ отлаживай на глазах у пользователя и не проси его «поменять настройки». Одним сообщением: что именно невозможно (одна фраза, без внутренних деталей) + человеческий обходной путь. Точка.
- Своя инфраструктура (что стоит на машине, почему инструмент не грузится, правки конфигов) — не тема разговора с пользователем. Нашёл проблему — запиши в память для администратора и работай тем, что есть.
<!-- noheavy:end -->
